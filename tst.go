package main

import (
	"github.com/urfave/cli"
	"log"
	"os"
)

func init() {
}

func main() {
	// App Creation
	//==================================================================================================================
	app := cli.NewApp()
	//==================================================================================================================

	// Flag Definitions
	//==================================================================================================================
	app.Flags = []cli.Flag{}
	//==================================================================================================================

	// App Action
	//==================================================================================================================
	app.Action = func(c *cli.Context) {
	}
	//==================================================================================================================

	// App Run
	//==================================================================================================================
	err := app.Run(os.Args)
	// Only run if something real weird happens
	if err != nil {
		log.Fatalln(err)
	}
	//==================================================================================================================
}
